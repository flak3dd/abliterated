# Abliterated IDE audit — 2026-09-05 (Melbourne / AEST)

## Summary
Full drift sync + harden. tsc/build green. Fixed StrictMode WS race (Bridge disconnected on load). Added ErrorBoundary, global error toasts, SSE tool_choice fallback, clearer offline errors, StatusBar guards, localStorage recovery. No secrets committed.
## Re-verify
- Box tsc PASS; Box build PASS; Mac tsc PASS
- vite 5173 PASS; bridge PASS; smoke PASS; p1-qa PASS
- fake-tool-calls/mcp-smoke deferred

## 1. Drift
Mac->box then harden box->Mac. Harden files: ErrorBoundary main sse storage bridgeClient StatusBar bridge.js index.css

## 2. Runtime scripts
- smoke PASS, p1-qa PASS

## 3. Browser console
Before: pageerror Bridge disconnected StrictMode WS race.
After: pageErrors none. Residual WS warning deferred. UI ok.

## 4. Vite stderr
Vite on 5173. No ECONNREFUSED spam during nav. Deferred: stderr capture.

## 5. Harden matrix
- ErrorBoundary: FAIL->FIXED
- global error toast: FAIL->FIXED
- SSE offline/HTTP + tool_choice fallback: FIXED
- Agent status=error: PASS already
- delete_file always replies: PASS
- StatusBar missing props: FIXED
- localStorage JSON recovery: FIXED
- Bridge WS StrictMode race: FAIL->FIXED

## 6. Type hygiene
tsc PASS box+Mac. Toast CSS added.

## 7. Deferred
- Residual StrictMode WS warning
- Re-run fake-tool-calls/mcp-smoke
- Vite stderr capture
- Side effect: playwright install under pw-audit; check home package.json if odd

## 8. Success
Report done. tsc/build green. ErrorBoundary + tool_choice fallback + agent error surfacing present. No secrets.
